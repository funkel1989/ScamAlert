@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        var returnUri = Uri.EscapeDataString(Nav.Uri);
        Nav.NavigateTo($"/login?returnUrl={returnUri}");
    }
}
